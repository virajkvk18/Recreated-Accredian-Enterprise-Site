import './globals.css';

export const metadata = { title: 'Accredian Enterprise', description: 'Next-gen expertise for your enterprise.' };
export default function RootLayout({ children }) {
  return <html lang="en"><body>{children}</body></html>;
}
